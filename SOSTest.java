import static org.junit.Assert.*;

/* 

public abstract class SOSTest {
    Assert newGame();
    Is red Points == 0
    Is blue Points == 0
    Is gameMode == -1
    Is gameOver == false
    Assert button pressed generalMode
    Is gameMode == 0
    Assert Grid Size = "akljfelkjel"
    Error?
    Assert buttonPressed twice
    Error?
    Assert exitButton
    Error?
    Assert PlayerVsPlayer Button
    error?
    Assert redPoints = 1
    Is redPoints > 0
}
*/
